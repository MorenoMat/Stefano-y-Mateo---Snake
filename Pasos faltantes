

FASE 1 — Snake en C++ (NO básico)

Objetivo: que el proyecto demuestre diseño, lógica y criterio, no solo que “funcione”.

1️⃣ Arquitectura mínima obligatoria

Si tu Snake está todo en main.cpp, es básico.
Tiene que tener separación clara.

Clases mínimas:

Game → controla el loop principal

Snake → cuerpo, movimiento, crecimiento

Board / Grid → límites, colisiones

Food → spawn y lógica

Input → manejo de teclas

Score → puntaje y estado

👉 Esto ya demuestra POO real.

2️⃣ Reglas claras y estados

Tenés que manejar estados del juego, no solo un loop infinito.

Ejemplo:

START

RUNNING

PAUSED

GAME_OVER

Un enum bien usado suma mucho.

3️⃣ Lógica no trivial

Para no quedar básico, el Snake debe incluir:

Colisión con bordes

Colisión consigo mismo

Crecimiento dinámico

Velocidad progresiva

Spawn de comida que no aparezca sobre la serpiente

Reinicio limpio del juego

4️⃣ Diseño limpio

Un reclutador técnico se fija en esto:

Métodos cortos

Nombres claros

Sin variables globales

Uso de const

Uso de referencias

Encapsulación real

No hace falta patrones complejos, pero orden sí.

5️⃣ Extras que lo suben de nivel (no obligatorios)

Elegí 1 o 2, no todo:

Guardado de high score en archivo

Configuración del juego (velocidad, tamaño)

Modo pausa

Tests simples de lógica (si te animás)

🟢 FASE 2 — SQL aplicado (cuando Snake esté firme)

Objetivo: mostrar que sabés persistir datos, no solo jugar.

1️⃣ Qué agregarle al Snake con SQL

No estudies SQL aislado. Integralo.

Funcionalidad ideal:

Usuarios

Partidas

Puntajes

Fecha y duración

Ejemplo de tablas:

users(id, username)
games(id, user_id, score, duration, date)

2️⃣ Operaciones mínimas obligatorias

Tenés que demostrar:

INSERT → guardar partidas

SELECT → ranking

WHERE → filtros

ORDER BY → top scores

JOIN → usuario + partidas

Con eso ya sos backend junior.

3️⃣ Tecnología recomendada

Para no complicarte:

SQLite (archivo local)

C++ o Python como puente

Queries escritas a mano (nada de magia)

🟢 FASE 3 — Diseño + pensamiento backend

Esto es lo que te diferencia.

Separar lógica del juego y persistencia

Clase Database

Métodos claros:

saveGame()

getTopScores()

Manejo de errores (DB caída, archivo inexistente)
